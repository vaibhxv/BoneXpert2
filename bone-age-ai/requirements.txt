# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- AI / inference ---
torch>=2.2.0
# The published models ship custom modeling code targeting Transformers 4.x;
# Transformers 5.x changed internal APIs and breaks trust_remote_code loading.
transformers>=4.44.0,<5.0.0
timm>=1.0.0
safetensors>=0.4.3
huggingface-hub>=0.24.0

# --- Image processing ---
numpy>=1.26.0
pillow>=10.3.0
opencv-python-headless>=4.9.0
scikit-image>=0.23.0
# Required by the published models' bundled preprocess() (modeling.py).
albumentations>=1.4.0

# --- Optional DICOM support ---
pydicom>=2.4.0

# --- Utilities ---
loguru>=0.7.2

# --- Testing ---
pytest>=8.2.0
httpx>=0.27.0
