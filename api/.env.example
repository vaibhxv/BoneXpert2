# NestJS API configuration

# HTTP port the NestJS API listens on.
PORT=3000

# Base URL of the Python bone-age inference service.
PYTHON_SERVICE_URL=http://localhost:8000

# Timeout (ms) for calls to the Python inference service.
PYTHON_SERVICE_TIMEOUT_MS=30000
